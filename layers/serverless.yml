service: sls-url-shortener-layers
app: serverless-url-shortener
org: toporok

frameworkVersion: '2'

provider:
  name: aws
  runtime: python3.8

  stage: ${opt:stage, 'dev'}
  region: us-east-1

layers:
  vendorDefault:
    name: ${self:service}-${self:provider.stage}-vendorDefault
    path: vendor-default

#resources:
#  Outputs:
#    VendorDefaultLayerExport:
#      Value:
#        Ref: VendorDefault-${self:provider.stage}
#      Export:
#        Name: VendorDefault-${self:provider.stage}
